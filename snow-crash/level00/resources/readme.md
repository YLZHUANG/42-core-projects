we use `find / -user flag00` to scan the target acount
we find two file `/usr/sbin/john` & `/rofs/usr/sbin/john`
both containing the `cdiiddwpgswtgt` string
with the `dcode.fr` website we can try to find the encryption metode, we a bit of try and error we found César code give `nottoohardhere` password
with `getflag` we get the flag